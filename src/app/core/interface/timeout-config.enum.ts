export enum GlobalTimeoutConfig {
  idleLimit = 30,
  idleWarning = 15
}

export enum HomeTimeoutConfig {
  idleLimit = 20,
  idleWarning = 10
}

export enum RegisterTimeoutConfig {
  idleLimit = 60 * 10
}
